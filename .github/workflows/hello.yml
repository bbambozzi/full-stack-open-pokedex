name: hello world

on:
  push:
    branches:
      - "master"
      - "main"
jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: say hello world
        run: |
          echo 'Hello, world!!'
